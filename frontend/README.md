# Frontend

React web application for the modular operations platform. V1 is a responsive browser application, not a PWA.

The supported browser baseline is Chrome on all supported devices.

Run:

- `npm run dev`
- `npm run build`
- `npm run lint`
- `npm run test`

The shell is route-aware, supports Arabic RTL and English LTR, and uses backend bootstrap data when available.
