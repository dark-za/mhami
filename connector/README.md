# Tenant AI Connector

Reserved for the future Docker-on-Linux connector installed by a customer technical team. It will execute tenant-selected private or local AI provider calls without granting the shared SaaS runtime direct access to tenant private networks.

The authoritative connector requirements are in `docs/contracts/CONNECTOR_CONTRACT.md` and Phase 09.
