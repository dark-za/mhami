# تقرير إعادة الفحص الأكاديمي

**تاريخ الفحص:** 28 أغسطس 2026  
**النطاق:** النسخة الحالية في `E:\APP\mhame\mhami-main`  
**المرجع:** وثائق المشروع الرسمية، مع مراعاة متطلبات الجاهزية النظامية السعودية  
**نوع الفحص:** تحليل ساكن شامل للقراءة فقط

لم تُشغّل الحاويات أو الاختبارات أو فحوص الشبكة، لذلك لا تُعد نتائج التشغيل موثقة ديناميكياً. التقييم القانوني تقني أولي وليس رأياً قانونياً معتمداً.

## القرار التنفيذي

**التقييم النهائي: 3.3/10**

**قرار اللجنة: NO-GO بالإجماع**

المشروع يمثل نموذجاً تقنياً واسعاً ومنظماً نسبياً، لكنه حالياً:

- لا يعرض الواجهة الرئيسية بسبب خطأ Router مؤكد.
- لا يبني صورة frontend الإنتاجية.
- لا يقلع Production Compose كما هو موثق.
- يحتوي مسارات أعمال أساسية مكسورة، خصوصاً exports وbackups.
- يحتوي ثغرات عزل وصلاحيات عالية الخطورة.
- لا يملك دليلاً على استخدام pilot فعلي.
- غير جاهز للامتثال السعودي أو لمعالجة بيانات شخصية حقيقية.
- المرحلة 12 ما زالت `NO-GO` والمرحلة 13 غير مبدوءة.

# النتائج الحرجة

## 1. الواجهة لا تقلع

يوجد `BrowserRouter` في موضعين:

- `frontend/src/main.tsx:14-18`
- `frontend/src/App.tsx:160-165`

React Router يرفض Router داخل Router. النتيجة المتوقعة هي انتقال التطبيق مباشرة إلى `AppErrorBoundary` بدلاً من عرض صفحات النظام.

الاختبار الوحيد للواجهة لا يكتشف ذلك لأنه يختبر `AppShell` منفرداً:

- `frontend/src/tests/app.test.tsx:8-25`

## 2. صورة frontend لا تُبنى

`npm run build` يشغّل:

```text
node scripts/check-generated-types.mjs
```

لكن `frontend/Dockerfile:5-12` لا ينسخ مجلد `scripts` قبل البناء.

المراجع:

- `frontend/package.json:8-9`
- `frontend/Dockerfile:16-18`

## 3. Production Compose لا يقلع

`AUDIT_HMAC_SECRET` إلزامي:

- `backend/config/settings/base.py:40-47`

لكنه غير ممرر في:

- `compose.prod.yml:7-13`
- `compose.prod.yml:29-35`
- `compose.prod.yml:51-60`
- `compose.prod.yml:88-97`

كما يحتاج NGINX شهادات TLS موجودة مسبقاً، بينما Compose ينشئ volumes فارغة ولا يحتوي provisioning للشهادات.

## 4. تسرب محتمل بين المستأجرين في النسخ الاحتياطية

خوارزمية النسخ تجمع السجلات بواسطة علاقات `OR` مع المستخدمين:

- `backend/apps/backups/services.py:73-127`

إذا كان مستخدم عضواً في شركتين فقد تتوسع نسخة الشركة الأولى إلى سجلات مرتبطة به في الثانية. كما تُنسخ إعدادات المنصة العالمية دون نطاق مستأجر:

- `backend/apps/backups/services.py:144-153`

هذا مانع إطلاق أمني مباشر.

# نتائج عالية الخطورة

1. الموظف العادي يستطيع اتخاذ قرارات مراجعة لعدم فرض دور owner/monitor:
   - `backend/apps/reviews/api/views.py:16-33`
   - `backend/apps/reviews/api/views.py:71-90`

2. تفاصيل الأدلة والبلاغات والرسائل لا تتحقق من نطاق الفرع:
   - `backend/apps/evidence/api/views.py:72-90`
   - `backend/apps/evidence/api/views.py:104-138`

3. قائمة فروع فارغة تعني كل فروع الشركة بدلاً من لا شيء:
   - `backend/apps/platform_core/querysets.py:45-57`

4. الحساب غير النشط يستطيع تسجيل الدخول والاستمرار في الجلسة:
   - `backend/apps/tenancy/auth_backends.py:12-45`

5. `active_until` للعضوية لا يدخل في قرارات الوصول:
   - `backend/apps/tenancy/access.py:44-64`

6. يمكن إنشاء وردية بعلاقات من شركة أخرى:
   - `backend/apps/organizations/api/views.py:96-125`

7. كشف الوجه يعتمد على قيمة يرسلها العميل:
   - `backend/apps/evidence/api/views.py:59-67`
   - `backend/apps/evidence/services.py:152-163`

8. CSRF غير مطبق في عميل الواجهة:
   - `frontend/src/api/client.ts:39-64`

9. API التصدير مكسور بسبب اختلاف توقيع الخدمة:
   - `backend/apps/exports/api/views.py:53-57`
   - `backend/apps/exports/services.py:215-234`

10. رابط تنزيل التصدير يمرر `token` بينما view ينتظر `request_id`.

11. API النسخ والاستعادة يمرر معاملات غير موجودة:
   - `backend/apps/backups/api/views.py:64-93`
   - `backend/apps/backups/services.py:285-304`

12. قائمة النسخ ترتب بواسطة `created_at` غير الموجود:
   - `backend/apps/backups/api/views.py:54-59`

13. المجدول غير مربوط فعلياً بـCelery Beat، ولا توجد جدولة overdue:
   - `backend/config/settings/base.py:209-226`

14. نقل المهمة لا يمسح `claimed_by` و`started_at`، ما يبقي صلاحية المنفذ السابق.

15. لا يوجد Linux tenant connector فعلي؛ `connector/README.md` يصفه كعمل مستقبلي.

# الاستخدام الفعلي

لا يوجد دليل قابل للتدقيق على استخدام فعلي مستدام.

ملفات `docs/pilot-evidence/01..08` الجديدة تعلن صراحة:

```text
UNFILLED TEMPLATE - NOT EVIDENCE
```

وجميع عناصر handoff ما زالت `pending`:

- `docs/pilot-evidence/08_PHASE1_HANDOFF_CHECKLIST.md:22-28`

لذلك:

- seed data ليست pilot.
- ملفات media ليست دليلاً على استخدام إنتاجي.
- الاختبارات ليست بديلاً عن فترة تشغيل بمستخدمين حقيقيين.
- لا توجد قياسات فعلية للسعة أو الاعتمادية أو قابلية الاستخدام.

# التقييم في 19 اختصاصاً

| # | القسم المتخصص | التقييم | الحكم |
|---:|---|---:|---|
| 1 | المعمارية وهندسة النظام | 5.0/10 | تقسيم modular جيد، لكن الحدود غير مكتملة |
| 2 | المنطق البرمجي وصحة الحالات | 3.5/10 | أخطاء في النقل والجدولة والمراجعة والتزامن |
| 3 | نمذجة البيانات والقيود | 4.0/10 | نماذج واسعة مع نقص invariants وقيود cross-tenant |
| 4 | اتساق API والعقود | 2.0/10 | exports/backups/OpenAPI والواجهة غير متطابقة |
| 5 | اكتمال وظائف Backend | 4.0/10 | قدرات كثيرة، لكن عدة مسارات أساسية معطلة |
| 6 | قابلية استخدام Frontend فعلياً | 1.0/10 | Router المتداخل يمنع الإقلاع |
| 7 | تجربة المستخدم والمنتج | 2.0/10 | shell تجريبي وتدفقات إدارية ناقصة |
| 8 | الوصولية والتجاوب والتعريب | 4.0/10 | RTL وتجاوب أساسي، لكن ترجمة ووصولية ناقصتان |
| 9 | الأمن التطبيقي | 5.0/10 | ضوابط مفيدة مع ثغرات صلاحيات وCSRF |
| 10 | الهوية وMFA والجلسات | 4.0/10 | MFA مشفر لكن غير مفروض والإبطال ناقص |
| 11 | عزل الشركات والفروع | 3.5/10 | IDOR وتسرب backup وعلاقات cross-tenant |
| 12 | حماية البيانات والخصوصية | 3.0/10 | private media جيدة جزئياً، الحذف والوجه ضعيفان |
| 13 | الجاهزية النظامية السعودية | 2.0/10 | النصوص والحقوق والاحتفاظ والنقل غير جاهزة |
| 14 | التشغيل والبنية التحتية | 3.0/10 | production topology غير قابل للإقلاع |
| 15 | CI/CD وسلسلة التوريد | 3.5/10 | CI موجود لكنه لا يثبت topology الإنتاجي |
| 16 | الاختبارات والجودة البرمجية | 4.5/10 | تغطية backend واسعة نسبياً مع 5 حالات فشل موثقة |
| 17 | المراقبة والسجلات والتنبيهات | 4.0/10 | metrics وقواعد alerts موجودة دون stack فعلي |
| 18 | النسخ والاستعادة والاستمرارية | 2.0/10 | محلي وغير مشفر ولا يمثل restore إنتاجياً |
| 19 | المتطلبات والحوكمة والاكتمال | 3.0/10 | اكتمال موثق مبالغ فيه وPhase 12 غير معتمدة |

**المجموع:** 63 من 190  
**المتوسط:** **3.32/10**

# تقييم الجودة

يوجد ساكناً:

- 91 دالة اختبار Backend موزعة على 27 ملفاً.
- اختبار Frontend واحد فقط.
- لا توجد اختبارات browser E2E.
- لا توجد اختبارات Compose أو NGINX أو TLS.
- اختبارات Backend تستخدم SQLite بدلاً من PostgreSQL الإنتاجي.
- لا توجد coverage gate.

القياسات المودعة تعترف بـ:

- 9 أخطاء mypy.
- نجاح 86 من 91 اختباراً فقط.
- 5 اختبارات فاشلة في exports/tasks/backups.
- تغطية docstrings تبلغ 6%.

المراجع:

- `docs/refinement/METRICS.md:47-76`
- `docs/refinement/METRICS.md:79-89`

وصف R-16 بأنه مكتمل أو أن الجودة كلها خضراء لا ينسجم مع هذه الأرقام.

# تقييم CI

ملف CI موجود فعلاً:

- `.github/workflows/ci.yml`

ويحتوي lint وmypy وpytest وCodeQL وaudits وSBOM لصورة Backend.

لكنه لا:

- يبني صورة frontend.
- يتحقق من `docker compose config`.
- يقلع topology الإنتاجي.
- يختبر NGINX أو TLS.
- يشغل أدوات docstrings والتعقيد وأحجام الملفات الجديدة.
- يطبق coverage threshold.
- يختبر PostgreSQL داخل pytest.
- يكتشف غياب أسرار Compose لأنه يمررها مباشرة إلى فحص Django.

# الامتثال السعودي

**الجاهزية: 2/10**

الموانع الأساسية:

- جميع الوثائق القانونية placeholders:
  - `docs/legal/README.md:3-5`
- `DATA_CLASSIFICATION.md` ما زال `Not started`.
- التسجيل ينشئ قبول الشروط والخصوصية آلياً:
  - `backend/apps/tenancy/services.py:136-147`
- لا توجد واجهة قبول قانوني فعلية.
- لا يوجد منع للاستخدام أو AI عند غياب القبول.
- لا يوجد purge للمستأجر بعد `pending_deletion`.
- ملفات التصدير لا تحذف عند انتهاء الصلاحية.
- لا توجد DSR أو ROPA أو DPIA.
- لا توجد قائمة معالجين فرعيين.
- لا توجد ضوابط مثبتة لنقل البيانات خارج المملكة.
- لا توجد آلية قانونية مكتملة للإبلاغ عن خرق البيانات.
- لا يوجد `LICENSE` أو `NOTICE` للمشروع.

# اكتمال الخطة الرسمية

| المرحلة | الحكم |
|---|---|
| 00 | Partial |
| 01 | Partial |
| 02 | Partial |
| 03 | أقرب إلى Complete ساكناً |
| 04-11 | Partial |
| 12 | Partial / NO-GO |
| 13 | Not started / Not evidenced |

نسبة الاكتمال المحافظة بحسب وجود الدليل: **نحو 50%**.

هذه النسبة لا تعني جاهزية إطلاق؛ جاهزية الإطلاق الخارجي تبقى صفراً حتى اعتماد خروج المرحلة 12 واجتياز المرحلة 13.

# قرار اللجنة المتخصصة

هذه **محاكاة لجنة مراجعة رسمية متعددة التخصصات** وليست اعتماداً صادراً عن جهة حكومية.

تشكيل اللجنة الافتراضية:

- رئيس حوكمة وإطلاق.
- معماري برمجيات.
- مهندس أمن تطبيقات.
- مختص IAM وعزل مستأجرين.
- مهندس جودة واختبارات.
- مهندس SRE وDevOps.
- مختص استمرارية أعمال وتعافي.
- مختص UX ووصولية.
- مسؤول خصوصية وامتثال سعودي.
- مراجع تراخيص وملكية فكرية.

**نتيجة التصويت:**

- الموافقة على Pilot حقيقي: **مرفوضة حالياً**.
- الموافقة على Phase 12 exit: **مرفوضة**.
- السماح ببدء Phase 13: **مرفوض**.
- السماح بإطلاق خارجي: **مرفوض بالإجماع**.
- السماح بمعالجة بيانات شخصية حقيقية: **مرفوض حتى إغلاق P0 والمراجعة القانونية**.

# شروط إعادة التقييم

1. إصلاح إقلاع الواجهة وبناء صورة frontend.
2. إصلاح Compose والأسرار والشهادات وhealthchecks.
3. إصلاح exports وbackups واختبار عقودهما طرفاً لطرف.
4. إغلاق ثغرات review/evidence/empty branches/cross-company shifts.
5. فرض `is_active` و`active_until` وإبطال الجلسات.
6. إعادة تصميم backup tenant selection والتشفير والوجهة الثانية.
7. تطبيق كشف وجه خادمي أو منع حفظ الصور غير المعالجة.
8. تطبيق CSRF ودورة جلسة وأدوار حقيقية في الواجهة.
9. جعل CI أخضر بالكامل وبناء topology الإنتاجي.
10. إكمال المتطلبات النظامية السعودية والحذف وDSR وDPIA وROPA.
11. تنفيذ pilot فعلي وتعبئة الأدلة الثمانية.
12. توقيع Phase 12 ثم تنفيذ بوابات Phase 13 كاملة.