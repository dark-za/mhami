# Contracts

These documents define stable external and module-boundary behavior before implementation. They are not executable protocol definitions yet; implementation must refine them without weakening the security requirements.
