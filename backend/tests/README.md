# Backend Tests

Reserved for cross-module tests. Module-specific tests will live alongside their modules when implementation begins.

The mandatory test strategy is defined in `docs/SECURITY_AND_DATA_BASELINE.md` and each phase exit criterion.
