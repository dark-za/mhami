# Grafana auto-provisioning for the bundled dashboards.
apiVersion: 1

providers:
  - name: mhami
    orgId: 1
    folder: mhami
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 60
    options:
      path: /var/lib/grafana/dashboards
